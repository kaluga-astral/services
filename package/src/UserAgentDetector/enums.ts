export enum OperatingSystem {
  Windows = 'Windows',
  MacOS = 'macOS',
  /** В т.ч. Linux, CentOS, Debian, Mint, RedHat, Ubuntu, SUSE, Unix, Solaris, AIX, FreeBSD */
  Unix = 'Unix',
  iOS = 'iOS',
  Android = 'Android',
  Unknown = 'Unknown',
}

export enum Browser {
  Chrome = 'Chrome',
  Yandex = 'YaBrowser',
  Opera = 'Opera',
  Firefox = 'Firefox',
  Safari = 'Safari',
  Edge = 'Edge',
  IE = 'IE',
  Unknown = 'Unknown',
}
